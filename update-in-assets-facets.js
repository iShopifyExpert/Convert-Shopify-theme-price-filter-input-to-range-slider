#find this this.querySelectorAll('input') and replace with this.querySelectorAll('input[type="number"]')

#find this const inputs = this.querySelectorAll('input'); and replace with const inputs = this.querySelectorAll('input[type="number"]');
